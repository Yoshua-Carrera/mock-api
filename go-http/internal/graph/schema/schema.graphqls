scalar JSON

type GenericMock {
  data: JSON!
  orchestratedMock: [JSON]
  mockStatusCode: Int
  error: [JSON]
  mockDelay: String!
  errorCode: Int!
}

type Mutation {
  mutateMock: GenericMock!
}

type Query {
  queryMock: GenericMock!
}
