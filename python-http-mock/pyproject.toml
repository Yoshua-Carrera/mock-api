[project]
name = "python-http-mock"
version = "0.1.0"
description = "A multiuse mock server written in python fast api"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.3",
]
